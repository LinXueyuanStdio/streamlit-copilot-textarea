import React from "react"
import ReactDOM from "react-dom"
import StreamlitCopilotTextArea from "./streamlit_copilot_textarea"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitCopilotTextArea />
  </React.StrictMode>,
  document.getElementById("root")
)
